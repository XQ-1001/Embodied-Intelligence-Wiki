# CLAUDE.md — 具身智能Wiki 工作规范

## 一、信息来源处理规范

### 1.1 网站/URL 身份验证协议

**问题**：此前因 WebSearch 未搜到某 URL 与公司的关联，就错误地否定了该 URL 是该公司官网，忽略了用户的直接指认。

**规范**：
- **WebSearch 用于发现，WebFetch 用于验证**。搜索不到不等于不存在。
- 当用户指出某个 URL 属于某公司时，**必须先直接用 WebFetch 访问该 URL**，查看页面内容确认，而不是依赖搜索结果的间接证据。
- 如果 WebFetch 被阻止，先排查配置问题（如 `~/.claude/settings.json` 中的 `skipWebFetchPreflight`），而非直接给出否定结论。
- 当用户提供的信息与搜索结果的"缺失"矛盾时，优先信任用户的直接知识。

### 1.2 引用来源必须实际读取

**问题**：将播客列为信息源，但从未实际读取/转写其内容，导致遗漏关键数据（融资总额）。

**规范**：
- 每个列在"参考资料"中的来源，**必须实际读取其内容**，不能仅凭摘要或简介就引用。
- 对于播客/音频类来源：必须下载音频并通过语音转写获取文字内容，不能仅引用 shownotes。
- 对于网页来源：必须用 WebFetch 直接获取页面内容，不能仅依赖搜索摘要。

### 1.3 官网域名验证（严禁推测）

**问题**：2026-05-19 发现微亿智造页面将官网 URL 错误标注为 `micro-eb.com`（从英文简称 "Micro-E Billion" 推测出来的域名），实际官网为 `microintelligence.com.cn`。`micro-eb.com` 实测连接失败（ERR_CONNECTION_CLOSED），属于推测代替验证导致的错误。

**规范**：
- **禁止从公司名称推测/推断域名**。英文名称缩写、拼音组合等推导结果必须经过实际访问验证。
- 页面中填写的"官网"字段如果是错误的，不仅误导读者，还会降低整页可信度。
- 新建或审核页面时，**对每个标注的官网 URL 必须至少用 WebFetch/curl 做一次实际连通性确认**（HTTP 200 即视为通过）。

### 1.4 多方交叉验证

- 同一事实尽量从多个独立来源验证（官网 + 媒体报道 + 工商信息 + 播客等）。
- 标注每条事实的来源可靠性（如"公司自述""媒体报道""工商登记""播客对话"等）。

### 1.5 招股书优先原则（已申报IPO的公司）

**问题**：2026-05-20 发现海康机器人页面数据源质量不足——对公司已向深交所提交招股说明书（申报稿）的情况，研究时未主动查找和下载官方招股书 PDF，而是止步于媒体文章对招股书的二次引用。用户直接给出了深交所官方 PDF 地址（`reportdocs.static.szse.cn`），暴露了研究流程中缺失的关键环节。

**规范**：
- 对于**已申报 IPO（含已受理/问询/过会/注册任一阶段）**的公司，**必须**从深交所/上交所/北交所/巨潮资讯网直接下载招股说明书（申报稿）PDF 原文。
- 招股书 PDF 下载渠道：
  - 深交所创业板：`https://reportdocs.static.szse.cn/UpFiles/rasinfodisc1/` 或巨潮资讯网 `cninfo.com.cn` 搜索公司名
  - 上交所科创板：`listing.sse.com.cn` 搜索公司名
  - 其他板块同理，优先搜索交易所官方披露平台
- 招股书是经保荐机构、律师、会计师三方背书的法律文件，其数据（营收、利润、毛利率、员工数、专利数、股东结构等）优先级**高于**任何媒体报道。
- 页面中涉及财务、团队规模、专利、股权结构的硬数据，**以招股书为第一来源标注**，媒体报道仅作为补充和交叉验证。
- 招股书通常数百页，提取数据时需用 PDF 解析工具（pdfplumber/PyPDF2）批量提取关键章节（如"发行人基本情况""财务会计信息""业务与技术"等）。

## 二、音频素材处理（播客/访谈/会议录音）

当列出的信息源涉及播客、访谈或会议录音时：
- **必须**对音频进行语音转写以获取文字内容，不得仅引用节目简介或 shownotes。
- 环境中已安装 `faster-whisper` 和 `ffmpeg`，具备本地转写能力。
- 中文内容优先使用中文语言模型转写，长音频需分段处理以免超时。
- 首次运行需要下载模型（缓存到 `~/.cache/huggingface/`），后续速度较快。

## 三、WebFetch 配置与故障排查

- 如果 WebFetch 无法访问某网站，优先通过 `~/.claude/settings.json` 配置 `skipWebFetchPreflight: true` 解决，不要因访问受阻就放弃获取信息。
- 科技媒体、主流新闻网站、公司官网、GitHub、Wikipedia 等默认应可访问。被阻止时先排查配置，而非给出否定结论。

## 四、错误复盘机制

### 4.1 当用户指出错误时

1. 立即承认错误，不要争辩或找借口
2. 追溯错误的根本原因（不是"我搞错了"，而是"我犯了什么逻辑错误/流程错误"）
3. 将原因和解决方案记录到 CLAUDE.md 中
4. 验证修复是否有效

### 4.2 常见错误模式（来自历史复盘）

| 错误 | 根因 | 解决方案 |
|------|------|---------|
| 否定官网身份 | 用"搜索没找到"替代"直接访问验证" | 1.1 协议 |
| 播客信息遗漏 | 列了来源但没实际读取 | 1.2 规范 |
| 官网URL写错 | 从公司英文名推测域名，未实际验证连通性 | 1.3 官网域名验证 |
| 数据无来源 | 引用公司/媒体数据时未标注出处 | 1.4 交叉验证 |
| 未使用招股书一手数据 | 已申报IPO的公司，研究时止步于媒体对招股书的二次引用，未下载招股书PDF原文 | 1.5 招股书优先原则 |
| 引用百度百科 | 百度百科不是一手数据源，内容可被任何人编辑 | 1.5 招股书优先原则（有招股书时用招股书，无招股书时用工商登记/官网/权威媒体报道） |
| 投资机构被投组合严重遗漏 | （1）未将官网投资组合页作为权威数据源优先提取；（2）SPA 渲染失败后放弃官网提取，降级为关键词搜索；（3）被纠正后未做全量审计，补一个算一个 | 1.6 投资组合提取协议 |

### 1.6 投资机构——官网投资组合提取协议

**问题**：2026-08-04 发现绿洲资本页面具身智能被投公司严重遗漏——初次只写了逐际动力 1 家，被纠正后补到 3 家，但仍然遗漏了苏度科技、乐享科技、纵贯创新、极壳科技、吾云创新、SHARGE、SPEEDIANCE 共 7 家（官网共列 10 家）。三次迭代才凑齐，根因是研究流程把"搜索结果"置于"官网完整列表"之上。

**规范**：

1. **官网投资组合页是投资机构页面研究的最高优先级数据源。** 先完整提取，再用搜索补充细节，不能反过来。
2. **当官网为 SPA（单页应用）时，WebFetch/curl 返回空壳 HTML 是预期行为，不是放弃理由。** SPA 的 JS bundle 中通常包含完整数据。标准应对流程：
   - 步骤 1：`curl -sL "https://{官网}/Companies"` 获取 HTML
   - 步骤 2：从 HTML 中提取 JS 入口文件路径（`grep -oP 'src="/assets/[^"]+\.js"'`）
   - 步骤 3：`curl -sL "https://{官网}/assets/{入口文件}.js"` 下载 JS bundle
   - 步骤 4：在 JS 中 grep 公司名称数据（中文名/英文名/股票代码等模式）
   - 步骤 5：如果入口 JS 是 router，从代码中找到 Companies 页面对应的 chunk URL（如 `import("./index-XXXXXX.js")`），下载该 chunk 获取完整列表
3. **提取完成后必须做两件事**：
   - a. **计数**——官网这个分类一共列了多少家公司？写进页面前先确认总数。
   - b. **全量交叉验证**——把官网列表中的每家公司，逐一搜索融资新闻确认绿洲的投资角色。能确认的标注"交叉验证"，确认不了的标注风险提示。
4. **被用户纠正遗漏时，不要只补用户指出的那一家。** 必须回到官网做完整审计——把所有被投公司重新拉一遍，逐一对比页面已有数据，找出全部遗漏。
5. **投资数据库（pedaily/lieyunpro/企查查）的机构主页比通用搜索更可靠**，应作为补充渠道使用：`site:pedaily.cn "{机构名}"` 或直接访问 `https://zdb.pedaily.cn/company/show{id}`。

## 五、Wiki 数据质量标准

- 每条事实必须在页面中标注来源编号（`<sup><a href="#refN">[N]</a></sup>`）
- 同一事实在页面中只有一处规范位置
- 公司自述数据与独立验证数据要区分标注
- 模糊/不确定的数据需在分析层标注审慎提示
- 更新页面后同步更新 index.html（排序、编号、统计、标签）和 tracker.csv
